apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontapp-ingress
spec:
  rules:
  - host: other.cubix.localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontapp
            port:
              number: 80
